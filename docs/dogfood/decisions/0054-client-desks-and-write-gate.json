{
  "pr": 54,
  "branch": "claude/client-intake",
  "date": "2026-08-06",
  "note": "A client's two desks, the scaffolding extracted from them, the fixture they produced, and the PreToolUse write gate the seat was missing. Numbered 0054 for the PR it opened as \u2014 written first as 0053, which #53 took. That is twice in one session that a number claimed before the PR existed had to be corrected, which is why the rule is to number for the PR it actually lands as.",
  "decisions": [
    {
      "question": "Bring the two-desks work over by copying the fixture, or by extracting the scaffolding?",
      "commitment": "Extract. demo/desks.py holds how to stand a desk up and how to say a claim failed, and knows nothing about whose desk it is.",
      "why": "The persona and the findings belong to the branch that earned them, and copying the fixture would have this branch carrying an Attercliffe walk-through nobody asked it to review. It also would have duplicated the machinery in two places before either had merged. Extracting means the client fixture starts from a module with a test rather than from a file it has to gut."
    },
    {
      "question": "What is the module actually for, if the demo it came from is not here?",
      "commitment": "The three process globals: the ledger path, the store and the matcher. Desk.activate sets all three together and every accessor calls it first.",
      "why": "A fixture that stands two desks up and sets these once has not built two desks \u2014 it has built one desk that changes its mind about which store it is. The failure is quiet: both desks' decisions land in whichever chain was configured last, and any count taken afterwards is a true number about the wrong thing. That is not hypothetical; it is what the first fixture written against this arrangement did, and it reported one desk's total as the other's. Switching desks is now one call you cannot half-do rather than three you can."
    },
    {
      "question": "Why install the matcher process-wide rather than only passing matcher= to each call?",
      "commitment": "Process-wide, in activate().",
      "why": "nestor.ui has no matcher of its own and no field for one, so every write the surface makes normalizes with whatever is installed. Passing matcher= covers the library calls a fixture makes and misses every seal and rejection made at the surface \u2014 which is where the human decisions are, and therefore the only ones worth a fixture. The constraint that comes with it is stated in the module docstring rather than hidden: one process holds one matcher, so two custom-matcher desks in one interpreter are two desks taking turns, and scripts/two_instances.py is what to reach for when that is not good enough."
    },
    {
      "question": "Does the module get a test when it has no fixture using it yet?",
      "commitment": "Yes \u2014 tests/test_desks.py, six tests, including one that reproduces the failure the module exists to prevent.",
      "why": "Otherwise it is scaffolding with no consumer and nothing holding it to its claims until the client fixture arrives. The last test sets the globals directly, the way a fixture that had never heard of Desk would, and asserts both desks' decisions land in one chain \u2014 so the thing being prevented is executable rather than described."
    },
    {
      "question": "The client's fixture \u2014 a scripted walk-through like shoebox.py, or something else?",
      "commitment": "Something else. demo/big_jim.py is a standing desk driven a command at a time, not an argument with an ending.",
      "why": "shoebox.py and sixty_seconds.py both make a case and stop; their value is that they assert and exit non-zero. This one is for a person playing the verifier, so it needs a store that survives between runs and a surface that takes decisions when they arrive rather than in a fixed order. It is the first fixture here that is a tool rather than a demonstration, and it is the first consumer of demo/desks.py \u2014 which is how the scaffolding got exercised by something other than its own test."
    },
    {
      "question": "How does the fixture avoid sealing on the operator's behalf?",
      "commitment": "seal requires --verifier on the command line: no default, no $USER fallback, and no route to sealed from draft().",
      "why": "TODO.md \u00a75.1 \u2014 a name a script supplied is not a human checking anything. The machine-side verb (draft) cannot reach status=sealed at all, and the human-side verb refuses to run without a name someone typed. On a used-car lot the incentive to blur that is unusually strong, which is the reason the fixture is set there."
    },
    {
      "question": "Why key on the last six of the VIN rather than the whole thing?",
      "commitment": "KEY_TAIL = 6, fixed before anything was measured, and the collision risk is reported by `state` rather than hidden.",
      "why": "Nobody on a forecourt reads all seventeen characters, and the last six are the sequential production number \u2014 the part that actually distinguishes two cars off the same line. It is a real trade-off, not a safe default: two vehicles can share a tail, and a disclosure served for the wrong car is the exact failure this product exists to prevent. So `state` counts keys held by more than one row and prints them in red. A number tuned until the demo looked good would be corpus-fitting wearing a method's clothes."
    },
    {
      "question": "Where does the review desk's list of defects come from?",
      "commitment": "Parsed from the `### 6.N` headings of IDEAS.md in this checkout, and nowhere else.",
      "why": "Same rule scripts/dogfood_store.py states and gates: a memory whose rows came from somewhere nobody can see is not an audit trail. A desk listing defects an agent remembered would be exactly that. An entry is open because its own heading says open, not because this module has a view \u2014 which is also why it disagrees with a naive grep: `grep -cE '^### 6.[0-9]+ .*open'` counts 11 because \u00a76.7's *title* is 'while the store is open', while the parser reads the status field and correctly counts 10."
    },
    {
      "question": "Does `bearing` \u2014 plain-English risk in, relevant defect out \u2014 actually work?",
      "commitment": "No, and the command says so on screen rather than presenting the ranking as an answer.",
      "why": "Measured: 'two people in the family share the same name' returns \u00a76.35 top at 0.062, when the entry actually about two people sharing a name is \u00a76.37. 'an alias for somebody nobody has verified' tops out at 0.062. The one query that scored (0.583) was phrased in the finding's own words. DefectMatcher weights identifier tokens above prose by design, and a risk described in plain English shares almost no identifiers with a defect title \u2014 so this is the matcher working as specified on input it was never meant for. It is left in because the browse view is still useful for finding candidates to read, and because a retrieval that scores 0.06 while looking confident is worth having on the record. Nothing is served: every row is a draft, so tier 1 is empty by construction and the bad ranking cannot reach an answer."
    },
    {
      "question": "The role-play session produced a fixture's worth of material. Write it up?",
      "commitment": "Yes \u2014 demo/filing_cabinet.py, ten beats, with tests/test_filing_cabinet.py.",
      "why": "It is the third fixture here and the first whose archive belongs to somebody with a reason to want the answer to come out a particular way. shoebox.py is a granddaughter after the truth about her grandmother: every incentive points one direction, and what it found were surfaces nobody had built. This one is a man whose interests and his record diverge, and it exercises the opposite half of the package \u2014 not what it shows but what it refuses, and the one thing it cannot refuse."
    },
    {
      "question": "What is the finding, as against the story?",
      "commitment": "There is no per-domain verifier policy. Measured: add_pair(status='sealed', verifier='anybody-at-all') is accepted and is_verified_seal returns True.",
      "why": "The client asked, in as many words, to be stopped from signing his own mileage rows \u2014 the one field three independent records in his file contradict him on. The package cannot do it: no required-signer list, nothing binding a domain to a name, and the only gate is possession of the seal key. That is defensible and it is not what was asked for, so the honest answer was to refuse and say the enforcement is a key he must give away and keep no copy of. No IDEAS number claimed: \u00a76.40 and \u00a76.41 are taken on claude/nestor-intake-repo-review-icanwi, and PR #52 has already shown what happens when a number is claimed before it lands. The argument is in the module docstring until the numbering is settled."
    },
    {
      "question": "Do the fixture's three gap assertions hold anything?",
      "commitment": "Proven by mutation, and the second mutation found the gate was broken.",
      "why": "Giving EntityResolver a `propose` turns \u00a76.39's assertion red. Making add_pair refuse a seal from an unenrolled verifier turns the policy assertion red \u2014 but only after a fix. The first version called add_pair unguarded, so the mutation made the fixture die with a traceback where it owed a GAP CLOSED. A gate that crashes instead of returning a verdict is not a gate; it now catches the refusal and reports it. This is the second time this session that mutation-testing found the defect in the test rather than in the code."
    },
    {
      "question": "Do those six tests hold anything?",
      "commitment": "Proven by mutation. Dropping the matcher line from activate() turns two red; dropping the ledger line turns three red.",
      "why": "A gate nobody has watched fail is a description. Also recorded: the first version of this file assumed a proposal writes a ledger line and asserted on chains after calling propose(). It does not \u2014 a draft is not a decision and appends nothing \u2014 so four tests failed for a reason that was the test's error rather than the module's, and they were rewritten to seal first. And the SerialMatcher used here keeps every digit it finds, so an early restatement of 'ward 6' keyed to 44716 and missed; that was the test's fault too, and the assertion now avoids stray digits and says why."
    },
    {
      "question": "The operator said the review-before-write hook was not working. Was it?",
      "commitment": "The hook worked. The hook they wanted did not exist.",
      "why": "hooks/nestor-hook exits 0 on both the claude and cursor paths and injected the seat context, including 'You may propose. You may not confirm.' \u2014 which was read and then not followed. What .claude/settings.json registered was SessionStart plus a PreToolUse matched on mcp__, and nothing on Write or Edit. So the mechanism fired and delivered advice, and advice gates nothing. That is IDEAS \u00a76.12's own thesis \u2014 the detection kit as gates, not advice \u2014 failing in the repository that wrote it. Also worth recording: this session started at 90e0eca, before PR #52 landed, so the old config was in force throughout regardless."
    },
    {
      "question": "The session-start script's unbound variable \u2014 cosmetic or real?",
      "commitment": "Real, and silent. Fixed by resolving the root once, guarded, in all three places.",
      "why": ".claude/hooks/session-start.sh ran `cd \"$CLAUDE_PROJECT_DIR\"` and wrote two PATH exports from the same bare variable, under `set -u`. hooks/nestor-hook is properly CLI-agnostic (NESTOR_PROJECT_ROOT -> CLAUDE_PROJECT_DIR -> CURSOR_PROJECT_DIR -> pwd) but does not export CLAUDE_PROJECT_DIR, and session_start.py invokes the script with check=False. So every non-Claude-Code invocation aborted at line 19, the failure was swallowed, and the hook still exited 0 \u2014 the venv bootstrap doing nothing while reporting success. The symptom is the exact trap the script's own header describes: a cold clone where pytest is missing. Reproduced before the fix and verified after, with the variable unset."
    },
    {
      "question": "Block, or warn?",
      "commitment": "Block, and redirect: the refusal names the two commands that clear it.",
      "why": "A refusal that only says no gets routed around, and this repo has an entry about that. The exit is doing the thing rather than giving up. Scope is deliberately narrow \u2014 Python under nestor/, recipes/, demo/, tests/, scripts/, hooks/, and nothing else. A gate that fires on a README edit is a gate people disable, and a disabled gate protects nothing. demo/review_desk.py is exempt because gating the tool that clears the gate is a deadlock discovered at the worst possible moment."
    },
    {
      "question": "What happens when the gate itself has a bug?",
      "commitment": "Fails closed on its subject, open on itself.",
      "why": "A missing or stale receipt denies \u2014 that is the point. An exception inside the hook allows the write, because a hook that wedges the session when its own parsing is wrong is a hook everybody deletes. Both defaults are asserted: malformed stdin returns allow, no consultation returns deny."
    },
    {
      "question": "Does the gate work?",
      "commitment": "Yes \u2014 it blocked the write of its own test file, in the session that built it.",
      "why": "Not a claim, an event: the Write of tests/test_before_write_gate.py came back BLOCKED, the desk was consulted as the message instructed, and the retry went through. Two bugs were found by testing rather than by reading. record() sat after cmd_bearing's early return, so a query matching nothing recorded nothing \u2014 and since `bearing` scores badly on plain-English risks, matching nothing is the common case, which would have made the gate unopenable exactly when it was hardest to satisfy. And the Claude deny payload is emitted in both spellings, {'decision': 'block'} and hookSpecificOutput.permissionDecision, because a gate that degrades to advice on half the installed base is the failure this hook exists to fix."
    }
  ]
}
